import React, { useState } from 'react';

import './NewPersona.css';

const NewPersona= (props) => {

  return (
    <div className='new-persona'>
     <button type='submit'>Add Persona</button>
    </div>
  );
};

export default NewPersona;
