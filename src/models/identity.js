class Identity {
    constructor(userId, id, name, type, profileImage) {
        this.userId = userId
        this.id = id
        this.name = name
        this.type = type
        this.profileImage = profileImage
    }
}

export default Identity