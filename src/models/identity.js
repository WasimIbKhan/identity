class Identity {
    constructor(id, name, type, profileImage) {
        this.id = id
        this.name = name
        this.type = type
        this.profileImage = profileImage
    }
}

export default Identity